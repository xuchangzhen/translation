# 复习卡滑动交互修复

Android 单词记忆 `0.4.3`（`versionCode=8`）和桌面端 `0.9.3` 修复了复习卡的横向浏览体验：

- 左滑进入上一张，右滑进入下一张；滑动不再直接写入“忘记”或“记得”的复习结果。
- 使用系统触控阈值加上更短的触发距离，横向手势会阻止外层滚动区域抢占事件，识别更灵敏。
- 卡片会跟随手指位移，完成切换时执行横向滑出和滑入动画；在第一张或最后一张继续滑动会回弹。
- 复习结果继续由四个评分按钮记录。已评分卡可向左回看，不会重复写入学习记录。
