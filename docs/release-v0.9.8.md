# Android 离线音标补全

Android 单词记忆 `0.4.9`（`versionCode=14`）和桌面端 `0.9.8` 新增自定义词库导入时的离线音标补全：

- 导入文件未提供 `phonetic` / `ipa` 时，会使用内置 ECDICT 精简词典进行标准化后的精确查询。
- 导入文件自带的音标优先，绝不会被本地词典覆盖。
- 预览与完成提示会分别显示已有、自动补全和未找到的音标数量；词典不可用或查不到时仍可正常导入，也不会影响复习进度。
