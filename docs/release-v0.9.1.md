# 词库界面更新

Android 单词记忆 `0.4.1`（`versionCode=6`）重新设计“我的词库”页面：

- 顶部保留“我的词库”和“导入词库”，导入入口改为明确的主操作按钮。
- “全部”和每个词库都使用独立卡片，显示来源、词条数、待复习数和新词数。
- 已选词库有颜色、边框和“已选择”状态；桌面自动接收和自定义词库有不同标识。
- 当前词库、开始复习、加密同步和词条列表各自形成清晰区块。
- 深色与浅色主题均已在 Android 模拟器验证。

本次为了使用既有三平台发布工作流，桌面安装包版本同步为 `0.9.1`；桌面功能没有新增改动。
