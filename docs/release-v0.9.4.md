# 复习卡堆叠滑动动画

Android 单词记忆 `0.4.4`（`versionCode=9`）和桌面端 `0.9.4` 将复习卡横向浏览改为更直接的堆叠式交互：

- 当前卡片会随手指完整跟随、轻微旋转和缩放；左侧或右侧目标卡在底层逐渐显现。
- 松手达到阈值后，当前卡横向滑出，目标卡平滑进入；不足阈值或在首尾继续滑动时会弹性回位。
- 左滑仍显示上一张，右滑显示下一张；评分仍只通过下方四个按钮写入学习记录。
