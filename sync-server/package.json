{
  "name": "@linguabridge/memory-sync-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zero-knowledge encrypted relay for LinguaBridge Android memory sync",
  "scripts": {
    "start": "node src/index.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "pg": "8.16.3"
  }
}
